const express = require("express");
const router = express.Router();

// TODO
// GET /status: get server status
// GET /health: get server health check
// GET /info: get server info (version, uptime, etc.)

module.exports = router;
